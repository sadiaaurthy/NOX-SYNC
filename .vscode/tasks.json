{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run FableOps",
            "type": "shell",
            "command": "./gradlew",
            "args": [":launcher:run"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
